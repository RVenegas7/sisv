# Python
__pycache__/
*.pyc
.venv/
backend/db.sqlite3

# Node
node_modules/
frontend/dist/
npm-debug.log*

# Entorno / secretos
.env
.env.local
# Credenciales del sistema legacy (solo plantillas versionadas)
legancy_conf/*.env
legancy_conf/tnsnames.ora

# Sistema
.DS_Store

# Contenido descartable (revisar antes de eliminar)
borrar/

# Dumps y backups del sistema legacy (exceden límites de GitHub / sin valor en VCS)
legancy/*.DMP
legancy/*.dmp
legancy/*.tgz
legancy/*.zip
legancy/*.ZIP

# Respaldos de las bases del proyecto (para la oficina/producción, no versionar)
dumps/

# Exportaciones de análisis con datos personales (cedulas/nombres del personal legacy)
candidatos_usuarios_desactivados.csv
centros_lara_status_I.csv